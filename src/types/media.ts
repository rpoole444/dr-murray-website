export type Video = { id: string; title: string; url: string; credits?: string };
export type Track = { id: string; title: string; src: string; credits?: string };
export type Photo = { id: string; src: string; alt: string; credit?: string };
